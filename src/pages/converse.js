// display conversation user list (or all)
// display conversation by id
// say as
// request generation by ai
// reorder conversation lines
// remove conversation lines
// create new line as copy of previous
import * as React from 'react';
import ReactDOM from 'react-dom';
import Button from '@mui/material/Button';

const ConversePage = ({ data }) => {
  return (
    <Layout>
        <Button variant="contained">Hello World</Button>
    </Layout>
  )
}

export default ConversePage
